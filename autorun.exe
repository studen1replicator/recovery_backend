#!/usr/bin/bash

sudo apt update
sudo apt install apache2 -y

echo "instalation complete"

#sudo mv /etc/apache2/sites-available/000-default.conf /root/
#sudo rm -f /etc/apache2/sites-enabled/000-default.conf /root/

#sudo cp ./config.new /etc/apache2/sites-available/000-default.conf
#sudo ln -s /etc/apache2/sites-available/project /etc/apache2/sites-enabled/project



sudo cp ./config.new /etc/apache2/sites-available/000-dafault.conf
sudo cp ./ports.new /etc/apache2/ports.conf

sudo systemctl restart apache2

echo
echo

ss -tnlp

echo
echo "script end of work"
